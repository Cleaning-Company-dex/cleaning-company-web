name: Debug PythonAnywhere Deployment

on:
  workflow_dispatch: # Permite ejecutar manualmente
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Test API Token
        env:
          PA_API_TOKEN: ${{ secrets.PYTHONANYWHERE_API_TOKEN }}
        run: |
          echo "🔍 Probando token de API..."

          # Verificar si el token existe
          if [ -z "$PA_API_TOKEN" ]; then
            echo "❌ PYTHONANYWHERE_API_TOKEN no está configurado"
            exit 1
          else
            echo "✓ Token encontrado (longitud: ${#PA_API_TOKEN} caracteres)"
          fi

          # Probar el token
          response=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Token $PA_API_TOKEN" \
            "https://www.pythonanywhere.com/api/v0/user/yerielcruz18/cpu/")

          if [ "$response" = "200" ]; then
            echo "✅ Token de API válido"
          else
            echo "❌ Token de API inválido o expirado (HTTP $response)"
            echo "Por favor, genera un nuevo token en:"
            echo "https://www.pythonanywhere.com/user/yerielcruz18/account/"
            exit 1
          fi

      - name: Check Website Status
        run: |
          echo "🌐 Verificando estado del sitio web..."

          # Intentar diferentes métodos de verificación
          echo "Método 1: curl simple"
          curl -I https://yerielcruz18.pythonanywhere.com 2>&1 | head -n 5

          echo ""
          echo "Método 2: wget"
          wget --spider -S https://yerielcruz18.pythonanywhere.com 2>&1 | head -n 10

          echo ""
          echo "Método 3: curl con user agent"
          response=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "User-Agent: Mozilla/5.0" \
            https://yerielcruz18.pythonanywhere.com)
          echo "Código de respuesta: $response"

      - name: Test API Endpoints
        env:
          PA_API_TOKEN: ${{ secrets.PYTHONANYWHERE_API_TOKEN }}
        run: |
          echo "🔌 Probando endpoints de API..."

          # Listar webapps
          echo "Obteniendo información de webapps..."
          curl -s -H "Authorization: Token $PA_API_TOKEN" \
            "https://www.pythonanywhere.com/api/v0/user/yerielcruz18/webapps/" \
            | python3 -m json.tool || echo "Error al obtener webapps"

          echo ""
          echo "Estado de las consolas..."
          curl -s -H "Authorization: Token $PA_API_TOKEN" \
            "https://www.pythonanywhere.com/api/v0/user/yerielcruz18/consoles/" \
            | python3 -m json.tool || echo "No hay consolas activas"

      - name: Repository Info
        run: |
          echo "📦 Información del repositorio:"
          echo "- Repositorio: ${{ github.repository }}"
          echo "- Branch: ${{ github.ref_name }}"
          echo "- Commit SHA: ${{ github.sha }}"
          echo "- Autor: ${{ github.actor }}"
          echo ""
          echo "📋 Archivos en el repositorio:"
          ls -la
          echo ""
          if [ -f "requirements.txt" ]; then
            echo "📦 Dependencias (requirements.txt):"
            cat requirements.txt
          else
            echo "⚠️ No se encontró requirements.txt"
          fi
